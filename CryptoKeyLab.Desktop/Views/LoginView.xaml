<Window x:Class="CryptoKeyLab.Desktop.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CryptoKeyLab.Desktop.Views"
        mc:Ignorable="d"
        Title="LoginView" Height="450" Width="600">
    

    <Grid>
        <!-- MAIN GRID: Splits the window into two big sections -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <!-- Left Section -->
            <ColumnDefinition Width="2*" />
            <!-- Right Section (wider) -->
        </Grid.ColumnDefinitions>

        <!-- 1. LEFT SIDE (Sidebar/Logo area) -->
        <Grid Grid.Column="0" Background="#2C3E50">
            <TextBlock Text="CryptoKeyLab" 
                   Foreground="White" 
                   FontSize="24" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center" 
                   TextWrapping="Wrap" 
                   TextAlignment="Center"/>
        </Grid>

        <!-- 2. RIGHT SIDE (Login Form) -->
        <Grid Grid.Column="1" Margin="40">
            <!-- NESTED GRID: Specifically for organizing the form items -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Title -->
                <RowDefinition Height="Auto"/>
                <!-- Username Label -->
                <RowDefinition Height="Auto"/>
                <!-- Username Field -->
                <RowDefinition Height="Auto"/>
                <!-- Password Label -->
                <RowDefinition Height="Auto"/>
                <!-- Password Field -->
                <RowDefinition Height="Auto"/>
                <!-- Login Button -->

                <!-- result Field -->
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Login" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Grid.Row="1" Text="Username" Margin="0,10,0,5"/>
            <TextBox Text="{Binding UserModel.UserName}"  Grid.Row="2" Padding="5"/>

            <TextBlock Grid.Row="3" Text="Password" Margin="0,10,0,5"/>
            <TextBox Text="{Binding UserModel.Password}" Grid.Row="4" Padding="5"/>

            <Button Command="{Binding AuthenticateLogin}" Grid.Row="5" Content="LOGIN" Margin="0,20,0,0" Height="35" Background="DodgerBlue" Foreground="White"/>

            <TextBlock Text="{Binding Result}" Grid.Row="6" Foreground="{Binding ResultColor}" Margin="0,10,0,0" TextWrapping="Wrap"/>
        </Grid>
    </Grid>

</Window>
