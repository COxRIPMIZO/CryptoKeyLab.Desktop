<UserControl x:Class="CryptoKeyLab.Desktop.Views.ApiKeyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CryptoKeyLab.Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="450">
   
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Foreground="Beige" HorizontalAlignment="Center" Text="API Key Management" FontSize="20" FontWeight="Bold" Margin="10"/>

        <StackPanel Grid.Row="2">
            <Button Content="Get your ApiKey" Height="40" Margin="3,3,3,3" FontWeight="Bold" FontSize="18"
                    Command="{Binding GetApiKeyCommand}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="10">
            <TextBlock Text="Your API Key:" FontSize="16" FontWeight="SemiBold" Margin="3,3,3,3"/>
            <TextBox Name="ApiKeyTextBox" Text="{Binding ApiKey.ApiKey}" IsReadOnly="True" FontSize="14" Padding="5" Margin="3,3,3,3"/>

            <TextBlock Text="Valid till:" FontSize="16" FontWeight="SemiBold" Margin="3,3,3,3"/>
            <TextBox Name="ApiKeyValidTillTextBox" Text="{Binding ApiKey.ExpireAt}" IsReadOnly="True" FontSize="14" Padding="5" Margin="3,3,3,3"/>

            <TextBlock Text="Your total usage:" FontSize="16" FontWeight="SemiBold" Margin="3,3,3,3"/>
            <TextBox Name="ApiKeyTotalUsageTextBox" Text="{Binding ApiKey.UsageCount}" IsReadOnly="True" FontSize="14" Padding="5" Margin="3,3,3,3"/>

            <TextBlock TextWrapping="Wrap" Text="{Binding ApiKey.Message}" Foreground="Red" FontSize="16" FontWeight="SemiBold" Margin="3,3,3,3"/>

        </StackPanel>

        <!--<local:ApiHealthStatusView Grid.Row="3" Height="30" Width="150" Margin="250,3,3,3"/>-->

    </Grid>
</UserControl>
